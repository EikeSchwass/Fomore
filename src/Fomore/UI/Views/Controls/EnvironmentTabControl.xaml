<UserControl x:Class="Fomore.UI.Views.Controls.EnvironmentTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:navigation="clr-namespace:Fomore.UI.ViewModel.Navigation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance navigation:EnvironmentTabVM}">
    <Grid>
        <StackPanel >
            <Button Content="Switch to Creature Tab" Command="{Binding NavigationVM.SwitchToCreatureTabCommand}" />
            <Button Content="Switch to Enviroment Tab" Command="{Binding NavigationVM.SwitchToEnvironmentTabCommand}"/>
            <Button Content="Switch to Training Tab" Command="{Binding NavigationVM.SwitchToTrainingTabCommand}"/>
            <Button Content="Switch to Simulation Tab" Command="{Binding NavigationVM.SwitchToSimulationTabCommand}"/>
            
            <Button Content="Add New Environment" Command="{Binding ShowEnvironCreationDialogCommand}" />
            <ListBox ItemsSource="{Binding Path=CreateEnvironDialogVM.getEnviron.Count}"/>
        </StackPanel>

        <Grid x:Name="CreatureNewEnvironment" Visibility="{Binding Path=CreateEnvironDialogVM.Visibility}">
            <Grid Background="Black" Opacity="0.5"/>
            <Border
                MinWidth="400"
                Background="{StaticResource Level2Brush}" 
                BorderBrush="Black" 
                BorderThickness="1" 
                CornerRadius="0,55,0,55" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center">
                <StackPanel Grid.Column="0">
                    <TextBlock Margin="5" Text="{Binding Path=Header}" FontWeight="Bold" FontFamily="Cambria" />

                    <TextBlock Margin="5" Text="{Binding Path=EnvironmentName}"/>
                    <TextBox x:Name="InputNameTextBox"  Padding="1" Margin="3" Text="{Binding Path=CreateCreatureDialogVM.EnvironmentName, Mode=TwoWay}"  />

                    <TextBlock Margin="5" Text="{Binding Path=Description}"/>
                    <TextBox  Padding="1" Margin="3" Text="{Binding Path=EnterDescription, Mode=TwoWay}"  />

                    <TextBlock Margin="5" Text="{Binding Path=Terrain}"/>
                    <TextBox  Padding="1" Margin="3" Text="{Binding Path=Terrain, Mode=OneWay}"  />

                    <TextBlock Margin="5" Text="{Binding Path=GroundType}"/>
                    <TextBox  Padding="1" Margin="3" Text="{Binding Path=GroundType, Mode=OneWay}"  />

                    <TextBlock Margin="5" Text="{Binding Path=Gravity}"/>
                    <TextBox  Padding="1" Margin="3" Text="{Binding Path=Gravity, Mode=OneWay}"  />

                    <TextBlock Margin="5" Text="{Binding Path=Friction}"/>
                    <TextBox  Padding="1" Margin="3" Text="{Binding Path=Friction, Mode=OneWay}"  />

                    <Button x:Name="Create" Margin="5" Content="{Binding Path=CreateButton}" Background="{x:Null}" Click="SaveButton_Click" />

                    <Button x:Name="Cancel" Margin="5" Content="{Binding Path=CancelButton}" Background="{x:Null}" Click="CancelButton_Click" />

                </StackPanel>
            </Border>
        </Grid>











    </Grid>
</UserControl>
